// const info = function (message) {
//   return `Hello ${message}`
// };

// const result = info("Rajan");
// console.log(result);

//Arrow function 

const info = (name, age) => {
  const info = `Hello ${name}`
  return info;
}

const result = info("Rajan");
console.log(result);
